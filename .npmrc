# pnpm configuration (see docs/tech-stack.md, phase-0-foundations 0.A)
# Reproducible installs: peers are installed automatically so a fresh checkout resolves
# the same graph. strict-peer-dependencies is kept off for now to keep the install green
# while the dev-tooling tree is young; tighten to `true` once the peer set is verified.
auto-install-peers=true
strict-peer-dependencies=false
