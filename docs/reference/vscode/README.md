# Reference — VS Code Extension

Specs for the VS Code extension — the in-editor surface for triggering workflows
on files. It is standalone: it bundles `packages/core` and runs workflows
in-process, so the desktop app is not required.

Part of [reference/](../README.md).

| File | Reference |
|------|-----------|
| [extension-api.md](extension-api.md) | The extension's commands, right-click triggers, status bar, sidebar, human-gate webview, and settings. |
