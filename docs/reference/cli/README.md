# Reference — CLI

Specs for the `relavium` command-line tool — the surface for CI/CD pipelines,
scripting, and power users. Built with commander.js + ink, it runs the same
`packages/core` engine as every other surface.

Part of [reference/](../README.md).

| File | Reference |
|------|-----------|
| [commands.md](commands.md) | The full `relavium` command surface (run, list, create, import, export, logs, gate, init, agent, provider) plus interactive TUI vs CI JSON mode. |
